dgraziotin/mysql is a fork of dgraziotin/lamp, and it is an
Out-of-the-box MySQL Docker image that *just works* on 
Mac OS X (and other platforms).
On OS X, it enables write support MySQL and MySQL mounted volumes.